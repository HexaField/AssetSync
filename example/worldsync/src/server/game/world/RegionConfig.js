export const RegionConfig = {
    regionSize: 256, // size of region
    regionDetail: 8, // number of triangles per region
    regionScale: 8, // scale regions up
    viewDistance: 32, // number of regions to load in each direction
    maxDepth: 3, // number of levels of detail iterations
}
