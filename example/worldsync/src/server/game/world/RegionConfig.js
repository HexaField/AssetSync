export const RegionConfig = {
    regionSize: 128, // number of triangles per region
    regionScale: 4, // scale regions up
    viewDistance: 8, // number of regions to load in each direction
    maxDepth: 1, // number of levels of detail iterations
}
