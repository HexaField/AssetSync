export const TerrainChunkConfig = {
    chunkSize: 512, // size of region
    chunkDetail: 16, // number of triangles per region
    // cacheDistance: 8, // number of chunks to pre-generate
    viewLevels: 6, // number of chunks to load in each direction for each level
    viewDistance: 8
}
