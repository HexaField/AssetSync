import test from 'ava'
// import Peer from './websocket/peer.js'
// import browser, { runServer } from './browser.js'

// const nodePeer = new Peer()
// await nodePeer.start()  

// const server = runServer('/websocket/index.html')

// test('Browser: slave process finds master process', browser, (t, page) => {
//     return new Promise((resolve) => {
//         nodePeer.on('start', (isMaster) => {
//             resolve(isMaster)
//         })
//     }).then((result) => {
//         t.false(result)
//     })
// })  

// test.after(() => {
//     server.close()
// })