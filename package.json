{
    "name": "AssetSync",
    "private": true,
    "workspaces": ["packages/*"],
    "scripts": {
        "addtoall": "lerna add $PACKAGE packages/*Sync --dev",
        "setup": "yarn install && lerna bootstrap",
        "test": "lerna run test",
        "build": "yarn test && lerna run clean:build && lerna run build",
        "example": "yarn setup && yarn build && cd examples/worldsync && yarn build"
    },
    "devDependencies": {
        "lerna": "^3.22.1",
        "npm-run-all": "^4.1.5"
    }
}
