{
    "name": "@AssetSync/common",
    "version": "0.0.0",
    "main": "src/index.js",
    "author": "HexaField <joshfield999@gmail.com>",
    "license": "MIT",
    "type": "module",
    "scripts": {
        "setup": "yarn install",
        "update": "yarn upgrade",
        "fix": "yarn audit fix",
        "test": "exit 0",
        "clean:build": "rm -rf dist",
        "build": "rollup src/index.js --format umd --name '@AssetSync/common' --file dist/index.js --inlineDynamicImports"
    },
    "devDependencies": {
    },
    "dependencies": {
    }
}
