{
    "name": "AssetSync",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "addtoall": "lerna add $PACKAGE packages/*Sync --dev",
        "setup": "yarn install && lerna clean --yes && lerna bootstrap",
        "test": "lerna run test"
    },
    "devDependencies": {
        "lerna": "^3.22.1",
        "npm-run-all": "^4.1.5"
    }
}